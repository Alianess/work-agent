# Friday 项目经理助理：自下而上建设 TODO

> 状态：方案已进入底座验证阶段。本文件记录已确认的建设顺序和验收边界；不代表已经接入 Plane、微信或图数据库。

## 开发原则

- 自下而上建设：先完成稳定的数据源、领域模型和查询接口，再叠加长期记忆、自动回忆和主动推进。
- 每一层都必须可以独立运行、测试、备份和迁移；上层只能通过稳定接口使用下层，不能直接读取内部表或依赖临时提示词。
- 不把开发过程建立在每一步都需要用户试用反馈之上。优先用接口契约、固定样例、自动化测试和可重复验收保证稳定性。
- 内部读取、检索和候选记忆抽取不逐次打扰用户；只有修改项目事实、对外发送消息或产生真实承诺时才要求确认。
- 原始材料永久保留且可追溯；摘要、向量、关系和长期记忆都是可重建的派生数据，不能替代原文。
- 当前任务压缩摘要只负责断点续作，不承担多年记忆、人物档案或项目事实库职责。

## 已确定的产品边界

- Friday 是项目经理助理的唯一持续对话入口；普通聊天用于展开独立任务，任务结果可以回到 Friday 继续讨论。
- 项目执行不在 Work Agent 内重造完整任务系统；先验证 Plane，AI 面向自有项目管理抽象接口，不直接绑定 Plane 内部实现。
- Plane 候选为项目执行事实源：项目、阶段、任务、节点、依赖、负责人、日期和状态。
- Work Agent 是项目情报事实源：原始材料、人物与公司、工作关系、调研、会议、承诺、决策依据和长期记忆。
- Plane 没有自然表达的风险、问题、决策、承诺、假设和变更，可以由 Work Agent 的项目管理扩展模型承载，并关联 Plane 对象。
- Friday 保持一个主助理；复杂工作由它按规则展开一个独立任务，不先引入 DAG 或复杂多智能体编排。
- 模型不能直接持有 Plane 万能凭证，也不能绕过受控接口修改项目或自动对外发送消息。

## 当前已有基础

- [x] 固定 Friday 持续对话入口，默认名称为 Friday，并支持修改名称。
- [x] 保留普通任务聊天，与 Friday 入口并存。
- [x] 当前任务上下文在约 `256,000 × 90%` 估算 Token 时触发压缩。
- [x] 压缩摘要最多 `8,192` Token；保留最近两轮用户问题和最终回答用于承上启下。
- [x] 摘要明确限定为当前任务断点续作，不作为长期记忆。
- [ ] 将以上工作树改动统一完成回归、提交和实际服务验证。

## 延期项：压缩分支式工作归档（暂不开发）

> 优先级：后续。它是上下文压缩成功后的独立分支，不阻塞压缩结果写回和当前聊天继续；不得改成每轮对话都执行的任务抽取。

- [ ] 压缩成功后分成两个互不混用的产物：
  - [ ] 续作摘要：覆盖更新，只负责继续当前手头任务。
  - [ ] 工作归档候选：异步提取本次压缩区间内新增的成果、决定、状态变化、待办、材料和来源。
- [ ] 归档分支先做范围判断：
  - [ ] `project`：能够高置信度定位到现有项目。
  - [ ] `standalone`：没有项目归属，但存在仍需关注的未完成事项或可复用成果。
  - [ ] `none`：查询、计算、改一句话、闲聊等无需结构化归档的小活，仅保留原始聊天和全文索引。
- [ ] 项目定位按确定性逐级执行：当前聊天绑定的 `project_id` > 项目正式名/别名精确匹配 > 材料路径或文件归属 > 人物、公司和事项联合匹配 > 全文/语义候选召回。
- [ ] 全局 Friday 维护可切换的“当前工作焦点”；出现明确项目切换信号时重新解析，不对每句话全库搜索。
- [ ] 定位结果有歧义时不得修改正式项目，先写入待归属区；后续上下文足以消歧时自动归并，仍不明确时再向用户确认。
- [ ] 不把海量小活建成任务列表：
  - [ ] 普通已完成小活只进入默认隐藏、可检索的 `WorkEpisode` 工作片段库。
  - [ ] 只有未完成、等待他人、存在截止日期或需要再次跟进的事项进入数量受控的进行中事项。
  - [ ] 跨多轮、多材料、多节点的事项才允许升级为项目，升级时保留原始来源和既有记录。
- [ ] 使用稳定 `project_id`、`work_key` 和来源消息范围进行幂等归并；同一工作继续推进时更新当前状态并追加阶段变化，没有新增事实时不写。
- [ ] 项目更新采用“来源消息 → 可审计变更事件 → 项目当前状态”的方式，保留旧值、新值、依据和撤回能力，不允许压缩分支静默覆盖项目事实。
- [ ] 历史定位先使用项目、人物、公司、时间、材料类型、状态等结构化过滤和 SQLite FTS5/BM25；真实评测证明必要后，再增加向量召回作为模糊搜索补充。
- [ ] 除压缩兜底外，未来可复用同一归档接口处理“形成最终材料/明确决定/工作完成”和跨日补存，但不得因此复制多套归档逻辑。

**验收：** 压缩失败不影响原始聊天；归档失败不影响续作摘要。项目能够稳定命中并幂等更新，歧义内容不污染正式项目，历史小活默认不占据任务界面但可以根据人物、项目、时间、材料和关键词准确找回。

## 自下而上的目标架构

```text
第 7 层  Friday 持续对话、主动推进、会议/周报/风险提醒
第 6 层  自动回忆 Context Hydration
第 5 层  混合检索与图增强检索
第 4 层  长期记忆：抽取、确认、纠正、失效、合并
第 3 层  材料与证据：原文、出处、时间线、权限
第 2 层  关系与情报中心：实体、关系、项目管理扩展对象
第 1 层  ProjectManagementPort + PlaneAdapter
第 0 层  Plane 项目执行基座
```

## Phase 0：验证 Plane 项目执行基座

- [ ] 在独立环境启动 Plane，不修改、不替换现有 Work Agent。
- [ ] 创建一个接近真实工作的样例项目，覆盖阶段/里程碑、任务、负责人、截止时间、依赖、风险和评论。
- [ ] 验证任务详情、看板、时间线/甘特、项目首页、筛选视图和移动端可用性。
- [ ] 验证 REST/MCP 最小闭环：读取项目、创建任务、更新状态、设置负责人和日期、建立依赖、添加评论。
- [ ] 验证 Webhook 的事件类型、重试行为、顺序、重复事件和断线恢复。
- [ ] 确认 Plane 对风险、问题、决策、承诺、变更和自定义字段的原生支持边界。
- [ ] 明确自托管资源、身份登录、数据目录、备份恢复、升级回滚和 AGPL 使用边界。
- [ ] 记录 Plane 对象 ID、Workspace/Project 权限和 API 限流规则。

**验收：** Plane 可以独立管理一个真实度足够的样例项目；API、Webhook、备份和升级路径均经过实际验证，尚未接触生产项目数据。

## Phase 1：ProjectManagementPort 与 PlaneAdapter

### 稳定领域模型

- [ ] 定义与具体产品无关的核心对象：
  - [ ] `Project`
  - [ ] `Stage` / `Milestone`
  - [ ] `WorkItem`
  - [ ] `Dependency`
  - [ ] `Member` / `Owner`
  - [ ] `ProjectSnapshot`
  - [ ] `ProjectChange`
- [ ] 定义项目经理扩展对象：
  - [ ] `Risk`
  - [ ] `Issue`
  - [ ] `Decision`
  - [ ] `ActionItem`
  - [ ] `Commitment`
  - [ ] `Assumption`
  - [ ] `ChangeRequest`
  - [ ] `Stakeholder`
- [ ] 明确每类对象的唯一 ID、状态机、责任人、时间字段、来源和审计字段。
- [ ] 明确哪些对象映射到 Plane，哪些由 Work Agent 扩展存储。

### AI 可用接口

- [ ] 定义只读接口：
  - [ ] `get_project_snapshot(project_id)`
  - [ ] `list_work_items(project_id, filters)`
  - [ ] `list_overdue_items(project_id)`
  - [ ] `get_dependency_chain(work_item_id)`
  - [ ] `get_recent_project_changes(project_id, since)`
  - [ ] `get_project_risks(project_id)`
  - [ ] `get_stakeholders(project_id)`
- [ ] 定义变更草案接口：
  - [ ] `propose_task_create(...)`
  - [ ] `propose_task_update(...)`
  - [ ] `propose_dependency_change(...)`
  - [ ] `propose_comment(...)`
  - [ ] `propose_project_extension_change(...)`
- [ ] 定义唯一确认写入口：`apply_approved_change(change_set_id)`。
- [ ] 模型只获得受限工具，不直接获得 Plane Token。

### 同步与事实边界

- [ ] 建立 Work Agent ID 与 Plane Workspace/Project/WorkItem ID 映射。
- [ ] Webhook 入库必须幂等，并记录事件 ID、发生时间、接收时间和处理结果。
- [ ] 保存同步游标、最近成功同步时间、失败原因和数据过期标记。
- [ ] Plane 与 Work Agent 数据冲突时显示冲突，不让模型自行覆盖。
- [ ] 每次真实变更记录发起聊天、来源材料、确认人、确认时间、Plane 返回值和失败原因。
- [ ] 为 Adapter 建立契约测试，使未来更换项目管理基座时不影响上层。

**验收：** Friday 可以通过稳定抽象读取完整项目快照，并把创建、改期、指派和催办整理为可核对的 ChangeSet；未经确认不能写入。

## Phase 2：关系与情报中心底座

### 数据模型

- [ ] 建立规范实体：人物、公司/组织、项目、地点、会议、材料、事件和项目管理对象。
- [ ] 建立实体别名、历史名称、外部 ID、同名消歧和重复实体合并机制。
- [ ] 每条关系必须包含：
  - [ ] 主体、客体、方向和关系类型
  - [ ] 业务发生时间、有效时间范围和系统记录时间
  - [ ] 来源与证据定位
  - [ ] 候选/已确认/已否定/已失效状态
  - [ ] 置信度、确认方式和确认人
  - [ ] 被哪条新关系纠正或替代
- [ ] 区分外部可验证关系与内部工作关系：
  - [ ] 工商、股权、法人/高管和公开合作关系记录公开来源与抓取时间。
  - [ ] 负责人、决策人、对接人、执行人、合作方、阻塞方和承诺人记录会议、聊天或文件证据。
- [ ] AI 自动抽取只能写入候选关系，不能直接成为已确认事实。
- [ ] 初期使用关系型存储维护规范实体和关系；是否增加 Neo4j 由后续多跳查询效果决定。

### 稳定接口

- [ ] `resolve_entity(name, active_project_id, context)`
- [ ] `get_entity(entity_id)`
- [ ] `list_relationships(entity_id, relation_types, time_range, status)`
- [ ] `get_project_stakeholders(project_id)`
- [ ] `get_local_relationship_graph(entity_id, hops, filters)`
- [ ] `propose_entity_merge(...)`
- [ ] `propose_relationship(...)`
- [ ] `confirm_or_reject_claim(claim_id, decision)`

### 可视化

- [ ] 项目干系人地图作为默认关系入口。
- [ ] 公司集团/工商关系树。
- [ ] 人物组织/上下级树。
- [ ] 项目依赖图，从 Plane 映射。
- [ ] 实体详情页：当前关系、历史变化、参与项目、材料时间线。
- [ ] 支持确认/驳回候选关系、合并重复实体、纠正关系和回看来源。
- [ ] 默认只显示局部一到两跳；支持按项目、关系类型、时间和确认状态筛选。

**验收：** 从项目、人或公司任一入口，都能稳定查询和可视化当前关系、历史变化与来源；关系数据可由 AI 受控查询和提出候选变更。

## Phase 3：材料与证据底座

- [ ] 建立统一 `SourceDocument` / `SourceEvent` / `EvidenceRef` 模型。
- [ ] 永久保存 Friday 对话、普通任务聊天、会议纪要/转写、文件和外部通道未来导入的原始内容。
- [ ] 原文与附件使用不可变版本或内容哈希；派生摘要不能覆盖原文。
- [ ] 每条材料记录来源类型、账号、项目、实体、发生时间、入库时间、权限范围和原文定位。
- [ ] 关系、决策、承诺、风险和记忆必须可以指回一条或多条证据。
- [ ] 在现有 SQLite FTS5/BM25 历史检索基础上建立统一关键词检索接口。
- [ ] 支持按项目、人物、公司、材料类型、时间和权限精确过滤。
- [ ] 建立账号与项目隔离测试，防止跨账号或跨项目召回。
- [ ] 建立导出、用户主动删除、备份和重建派生索引的能力。

**验收：** 任意结构化事实都能回到原始材料的准确位置；删除派生索引后可以从原始数据重建。

## Phase 4：长期记忆内核

### 记忆类型

- [ ] 情节记忆：某次会议、沟通、事件及其时间线。
- [ ] 语义事实：人物、公司、项目和经过确认的稳定事实。
- [ ] 关系记忆：人物、公司和项目之间有来源的关系。
- [ ] 项目记忆：决策、承诺、风险、问题、变更、未完成事项和推进历史。
- [ ] 用户工作偏好：汇报格式、沟通习惯和稳定约束。
- [ ] 程序性记忆：经过验证的工作流程、模板和 Skill 使用方法。

### 写入与演化

- [ ] 明确流水线：原始观察 → 候选 Claim → 证据聚合 → 确认事实/记忆。
- [ ] 新材料到达时只做候选抽取、实体链接和证据绑定，不直接改写确认事实。
- [ ] 支持重复合并、冲突并存、用户纠正、过期失效和新事实替代旧事实。
- [ ] 区分业务发生时间与系统知道该事实的时间，支持回看“当时知道什么”。
- [ ] 定期进行记忆整理与去重，但不删除原始材料，不依赖当前任务压缩摘要。
- [ ] 每条长期记忆记录来源、生成方式、最近验证时间和可用权限。

### AI 可用接口

- [ ] `remember_observation(source_id)`
- [ ] `list_candidate_claims(scope, filters)`
- [ ] `get_confirmed_memories(scope, query)`
- [ ] `get_project_memory(project_id, time_range)`
- [ ] `correct_memory(memory_id, correction, evidence)`
- [ ] `supersede_memory(old_id, new_id)`

**验收：** 同一事实经过多次聊天、会议和文件变化后仍可追踪来源、冲突与当前有效版本；当前任务摘要被清空也不影响长期记忆。

## Phase 5：混合检索与图增强检索

- [ ] 在关键词检索之上增加语义向量检索和重排。
- [ ] 结构化问题优先查询 PlaneAdapter、关系中心和长期记忆接口，不用向量搜索猜项目状态。
- [ ] “谁说过什么、材料如何约定”使用关键词 + 向量 + 元数据过滤，并返回原文片段。
- [ ] 多人、多公司、多项目关联问题才沿已确认关系进行一到两跳扩展。
- [ ] 图检索中的每条关键路径必须回取关系证据。
- [ ] 不把全量 LLM 自动抽图或社区摘要作为项目事实源。
- [ ] 材料规模和真实评测证明必要前，不引入完整 Microsoft GraphRAG 或独立图数据库。

**验收：** 能回答材料证据问题和跨实体影响问题，并清楚区分项目实时状态、确认事实、候选推断和原文证据。

## Phase 6：自动回忆（Context Hydration）

- [ ] 每轮用户输入识别人名、公司、项目、节点、事件、推进动词和代词指代。
- [ ] 根据当前项目、最近焦点和实体消歧结果决定查询范围。
- [ ] 自动组成固定预算的 Context Pack：
  - [ ] 当前项目快照与最近变化
  - [ ] 相关实体的一到两跳已确认关系
  - [ ] 决策、承诺、风险、问题和待办
  - [ ] 少量最相关的原始证据片段
- [ ] 排序基线：当前项目 > 明确提及实体 > 最近变化 > 截止/风险 > 语义相关材料。
- [ ] Context Pack 默认隐藏，可通过“依据/回忆”查看命中内容、来源和检索原因。
- [ ] 记录命中、遗漏、误召回和用户纠正，形成固定评测集。
- [ ] 建立准确率、错误注入率、出处覆盖率、延迟和 Token 消耗基线。

**验收：** 用户说“李明那边场地是不是又拖了”，Friday 无需用户主动要求检索，即可带入对应项目、李明角色、节点状态、依赖和最近证据。

## Phase 7：项目经理工作流与主动推进

- [ ] 根据项目到期、依赖变化、风险和承诺状态生成每日“需要你推动”的清单。
- [ ] 生成会议前背景、会后行动项、催办草稿、风险升级草稿、项目状态报告和领导周报。
- [ ] 给出“该找谁、该催什么、何时升级、需要谁决策”的项目经理建议。
- [ ] Friday 判断任务过重时，可以自行创建一个独立任务；主对话保留任务引用并接收最终结果。
- [ ] 默认只读取、提醒和拟稿；发送消息、修改项目状态和形成对外承诺必须确认。
- [ ] 所有主动判断都显示数据时间与依据，不把过期项目快照当成实时事实。

**验收：** Friday 能持续推进项目而不是等待用户逐项提问，同时所有真实写入和外部动作仍受控。

## Phase 8：微信与其他消息通道

- [ ] 定义与具体平台无关的 `MessageChannelAdapter`。
- [ ] 统一消息事件字段：通道、会话、发送人、时间、原始消息 ID、引用/转发关系、附件和权限。
- [ ] 微信接入单独验证官方能力、OpenClaw 插件或其他可持续方案。
- [ ] 在通道方案确定前，不预设“手机收件箱”或人工复制粘贴为正式路线。
- [ ] 消息接入后复用 Phase 3 的统一材料与证据入口，不再建立第二套入库逻辑。
- [ ] 对外回复、群发、转发和日程写入继续遵守确认与审计规则。

**验收：** 新消息通道只需实现适配器即可复用材料、关系、长期记忆和自动回忆能力。

## 跨阶段稳定性要求

- [ ] 数据库迁移可回滚，原始数据升级前自动备份。
- [ ] 所有核心接口有契约测试、权限测试、失败重试和审计日志。
- [ ] 所有派生数据均可从 Plane 与原始材料重建。
- [ ] 账号、项目和材料权限在存储、检索、关系扩展和 Context Pack 中一致生效。
- [ ] 服务中断、Webhook 延迟或外部系统不可用时明确显示数据可能过期，不静默编造。
- [ ] 每个 Phase 达到验收条件后再让上层依赖，不用临时兼容代码提前跨层开发。

## 架构红线

- 不把项目管理能力建立在聊天摘要或单纯 RAG 上。
- 不在 Work Agent 内重造完整甘特、看板和任务执行系统。
- 不让模型直接修改 Plane、确认结构化事实或自动对外发送消息。
- 不先上复杂 DAG/多智能体编排。
- 不把全局关系图作为默认界面。
- 不将 AI 推测混入工商事实或已确认项目事实。
- 不让上层 AI 绕过 Adapter 直接读取 Plane 数据库或关系中心内部表。
